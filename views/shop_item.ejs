<!DOCTYPE HTML>
<html lang="zh-cn">
<head>
    <meta charset="UTF-8">
    <title>餐库</title>
    <link href="/css/reset.css" rel="stylesheet" type="text/css"/>
    <link href="/css/style.css" rel="stylesheet" type="text/css"/>
    <script src="/js/jquery.js" type="text/javascript"></script>
    <script src="/js/jquery.reveal.js" type="text/javascript"></script>
    <!--[if IE]>
    <script src="/js/html5.js"></script><![endif]-->
</head>
<body>
<header id="head">
    <nav class="nav">
        <div class="wap">
            <div title="餐库" class="logo"><a href="/"><span>餐库</span></a></div>
            <ul>
                <li><a href="/today">今日订单</a></li>
                <% if (locals.session.user) { %>
                <li class="nav_item_my">
                    <a href='/logout'><%=locals.session.user.name%></a>
                    <ul id="nav_my">
                        <li><a href="/order">历史订单</a></li>
                        <li><a href="/logout">退出</a></li>
                    </ul>
                </li>
                <% } %>
            </ul>
        </div>
    </nav>
</header>
<div class="wap columns ">
    <div class="left">
        <h1 class="page-title">
            <strong><%=shop.name%></strong>
            <span>电话：<%=shop.tel%></span>
            <span>地址：<%=shop.address%></span>
            <input type="hidden" id="shop_id" value="<%=shop._id%>">
            <input type="hidden" id="shop_name" value="<%=shop.name%>">
        </h1>
        <section class="main clearfix">
            <section class="food-list clearfix">
                <% if(foods.length>0){%>
                <ul>
                    <% foods.forEach(function(food){%>
                    <li id="food-<%=food._id%>" data-id="<%=food._id%>" data-name="<%=food.name%>"
                        data-price="<%=food.price%>">
                        <%=food.name%><em><%=food.price%> 元</em></li>
                    <% })}%>
                </ul>
                <% if(foods.length<=0){ %>
                <p>还未添加美食</p>
                <%}%>
            </section>
        </section>
    </div>
    <div class="right">
        <section id="cart">
            <h3>我的餐盒</h3>
            <div class="r_mycart">
                <table cellpadding="0" cellspacing="0">
                    <tbody>
                    <tr>
                        <th class="ttl">名称</th>
                        <th width="40">份数</th>
                        <th width="30">单价</th>
                        <th class="del" width="30">删除</th>
                    </tr>
                    </tbody>
                </table>
                <p id="noItemTips" class="ptt pbb" style="text-align: center; display: none; ">您还没有添加菜品哦~</p>
                <table id="cartTable">
                    <tbody>
                    </tbody>
                </table>
            </div>
            <div class="buy"><span class="buy-btn">去结帐</span><strong class="buy-price">总价：<span id="cart_zongjia">0</span></strong>
            </div>
        </section>
    </div>
    <div id="car-confirm" class="reveal-modal">
        <h3>订单详情</h3>

        <div id="confirm-list"></div>
        <a class="close-reveal-modal">&#215;</a>
    </div>
</div>
<script type="text/javascript" src="/js/cart.js"></script>
</body>
</html>